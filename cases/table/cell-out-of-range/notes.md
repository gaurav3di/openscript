# table/cell-out-of-range

A grid of two rows written at row 2 on bar 6.

## What it pins

`errors.md` OS4008: rows count from zero, so row 2 of a two row grid is outside
it, and the bar stops at the call on line 8.

## Reference

By the rule above, by hand.
