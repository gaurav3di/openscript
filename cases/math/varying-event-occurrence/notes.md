# math/varying-event-occurrence

The first call records two true events and then asks for the older one on a
false bar. Its earlier zero occurrence cannot discard that value. The second
call counts every bar as an event and shrinks from occurrence one to zero;
the newest event is immediately available. The expected values follow directly
from event order, independently of either engine's internal retention.
