"""Programs that are about the engine rather than part of it.

Nothing here is installed with the package: ``pyproject.toml`` names one
package and it is not this one. A test may import from here, and the gate runs
``run_tests.py``; the engine never imports it.
"""
