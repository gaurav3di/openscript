{
  "name": "openscript-lang",
  "version": "0.0.0",
  "description": "An open trading language. Write a study or a strategy once, plot it, backtest it, trade it.",
  "type": "module",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "check:names": "node scripts/check-names.mjs",
    "check:examples": "node scripts/check-examples.mjs",
    "check:errors": "node scripts/check-error-codes.mjs",
    "test": "node scripts/check-names.mjs && node scripts/check-examples.mjs && node scripts/check-error-codes.mjs"
  },
  "keywords": [
    "trading",
    "language",
    "compiler",
    "indicators",
    "backtesting",
    "technical-analysis"
  ],
  "engines": {
    "node": ">=20"
  }
}
